s = str(input("Enter a string: "))
ch = str(input("Enter a character: "))
def count(s, ch):
    result = 0
    for x in s:
        if(ch == x):
            result += 1
    return result
print(count(s, ch))
